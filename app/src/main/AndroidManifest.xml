<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kevin.android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.FlowerActivity"
            android:label="仙女散花" />
        <activity android:name=".ui.SwipeRefreshLayoutActivity" />
        <activity android:name=".ui.EventDistributionActivity" />
        <activity android:name=".ui.viewgroup.DialogActivity"></activity>
    </application>

</manifest>